/**
 * The only approved ElevenLabs voice for JAC.
 *
 * This constant is intentionally shared by browser ConvAI startup and the
 * server-side TTS proxy so neither path can drift to a different voice.
 */
export const JAC_ELEVENLABS_VOICE_ID = "h2dQOVyUfIDqY2whPOMo";